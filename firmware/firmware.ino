// Primary Arduino sketch file for the firmware/ sketch folder.
//
// Arduino requires the primary .ino filename to match the sketch folder name.
// The implementation remains in mobile_substation_monitoring.ino so the project
// keeps its descriptive source filename while remaining compatible with
// Arduino IDE and Arduino CLI builds.
